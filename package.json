{
  "name": "config",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch ./config/db.js -p 3003"
  },
  "author": "ysknk",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.16.2"
  }
}
